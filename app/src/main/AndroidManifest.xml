<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- No INTERNET permission requested — app is fully offline by design -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.MedApp"
        android:extractNativeLibs="true"
        tools:targetApi="34">

        <activity
            android:name=".ui.screens.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.screens.ModelListActivity"
            android:exported="false"
            android:label="@string/select_model" />

        <activity
            android:name=".ui.screens.InferenceActivity"
            android:exported="false"
            android:label="@string/analyze_image" />

    </application>
</manifest>
